<UserControl x:Class="Torun.UControls.UCSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Torun.UControls"
             mc:Ignorable="d" Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0">
            <StackPanel>
                <TextBlock x:Name="titleMainCount" Text="XXX" Style="{DynamicResource textBlockTitle}"/>
                <GroupBox x:Name="groupMainCount" Header="XXX" Padding="20">
                    <ItemsControl>
                        <StackPanel>
                            <DockPanel>
                                <RadioButton Style="{DynamicResource completedRadios}" x:Name="radioDaily" GroupName="mainCount" Content="XXX" />
                                <RadioButton Style="{DynamicResource completedRadios}" x:Name="radioWeekly" GroupName="mainCount" Content="XXX" />
                            </DockPanel>
                            <DockPanel>
                                <RadioButton Style="{DynamicResource completedRadios}" x:Name="radioMonthly" GroupName="mainCount" Content="XXX" />
                                <RadioButton Style="{DynamicResource completedRadios}"  x:Name="radioBeforeStart" GroupName="mainCount" Content="XXX" />
                            </DockPanel>
                        </StackPanel>
                    </ItemsControl>
                </GroupBox>
            </StackPanel>
            
        </Border>
        <Border Grid.Column="1">

        </Border>
    </Grid>
</UserControl>
